[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "flowboard-api"
version = "0.1.0"
description = "FastAPI backend for Flowboard kanban app"
authors = ["Your Name <you@example.com>"]
